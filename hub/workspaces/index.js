// hub/workspaces/index.js — Session Hub Phase 7: Workspace watcher/conflicts (TDD v0.5 §21). Placeholder scaffold.
export default function workspacesPlaceholder() {
  throw new Error(
    'hub/workspaces is a placeholder scaffold (SH-0-01). ' +
      'Implement the workspaces watcher/coalescer/git-evidence modules before importing it.',
  );
}
