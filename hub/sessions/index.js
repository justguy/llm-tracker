// hub/sessions/index.js — Session Hub Phase 2: Session presence + dumb/manual control (TDD v0.5 §21). Placeholder scaffold.
export default function sessionsPlaceholder() {
  throw new Error(
    'hub/sessions is a placeholder scaffold (SH-0-01). ' +
      'Implement the session registry/adapters/auth/stdio modules before importing it.',
  );
}
